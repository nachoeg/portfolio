<div class="canvas">
  <div class="tracker"></div>
  <div class="tracker"></div>
  <div class="tracker"></div>
  <div class="tracker"></div>
  <div class="tracker"></div>
  <div class="tracker"></div>
  <div class="tracker"></div>
  <div class="tracker"></div>
  <div class="tracker"></div>
  <div class="slot">
    <slot />
  </div>
</div>

<style>
  .slot {
    grid-row: 1 / -1;
    grid-column: 1 / -1;
    position: relative;
  }

  .canvas {
    position: relative;
    inset: 0;
    z-index: 0;
    width: 100%;
    height: 100%;
    display: grid;
    align-items: center;
    place-self: center;
    grid-template-columns: repeat(3, 1fr);
    grid-template-rows: repeat(3, 1fr);
    outline: 5px solid #fff;
  }

  .tracker {
    position: absolute;
    width: 100%;
    height: 100%;
    inset: 0;
    border: 1px solid lime;
    transform: perspective(500px) rotateY(var(--rotateX))
      rotateX(var(--rotateY));
  }

  .tracker:nth-child(1) {
    grid-column: 1 / 2;
    grid-row: 1 / 2;
    z-index: 1;
    inset: 0;
  }

  .tracker:nth-child(2) {
    grid-column: 2 / 3;
    grid-row: 1 / 2;
    z-index: 1;
    inset: 0;
  }

  .tracker:nth-child(3) {
    grid-column: 3 / 4;
    grid-row: 1 / 2;
    z-index: 1;
    inset: 0;
  }

  .tracker:nth-child(4) {
    grid-column: 1 / 2;
    grid-row: 2 / 3;
    z-index: 1;
    inset: 0;
  }

  .tracker:nth-child(5) {
    grid-column: 2 / 3;
    grid-row: 2 / 3;
    z-index: 1;
    inset: 0;
  }

  .tracker:nth-child(6) {
    grid-column: 3 / 4;
    grid-row: 2 / 3;
    z-index: 1;
    inset: 0;
  }

  .tracker:nth-child(7) {
    grid-column: 1 / 2;
    grid-row: 3 / 4;
    top: 0;
    bottom: 65px;
    left: 0;
    right: 0;
    z-index: 1;
    /* inset: 0; */
  }

  .tracker:nth-child(8) {
    grid-column: 2 / 3;
    grid-row: 3 / 4;
    top: 0;
    bottom: 65px;
    left: 0;
    right: 0;
    z-index: 1;
    /* inset: 0; */
  }

  .tracker:nth-child(9) {
    grid-column: 3 / 4;
    grid-row: 3 / 4;
    top: 0;
    bottom: 65px;
    left: 0;
    right: 0;
    z-index: 1;
    /* inset: 0; */
  }

  .tracker:nth-child(1):hover ~ .slot #profileCard ~ .slot #profileCard {
    background-color: red;
    --rotateX: -25deg;
    --rotateY: 25deg;
  }

  .tracker:nth-child(2):hover ~ .slot #profileCard {
    background-color: red;

    --rotateX: 0deg;
    --rotateY: 25deg;
  }

  .tracker:nth-child(3):hover ~ .slot #profileCard {
    background-color: red;

    --rotateX: 25deg;
    --rotateY: 25deg;
  }

  .tracker:nth-child(4):hover ~ .slot #profileCard {
    background-color: red;

    --rotateX: -25deg;
    --rotateY: 0deg;
  }

  .tracker:nth-child(5):hover ~ .slot #profileCard {
    background-color: red;

    --rotateX: 0deg;
    --rotateY: 0deg;
  }

  .tracker:nth-child(6):hover ~ .slot #profileCard {
    background-color: red;

    --rotateX: 25deg;
    --rotateY: 0deg;
  }

  .tracker:nth-child(7):hover ~ .slot #profileCard {
    background-color: red;

    --rotateX: -25deg;
    --rotateY: -25deg;
  }

  .tracker:nth-child(8):hover ~ .slot #profileCard {
    background-color: red;

    --rotateX: 0deg;
    --rotateY: -25deg;
  }

  .tracker:nth-child(9):hover ~ .slot #profileCard {
    background-color: red;

    --rotateX: 25deg;
    --rotateY: -25deg;
  }
</style>
