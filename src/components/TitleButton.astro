---
const { title, classList } = Astro.props;
---

<button
  class=`ms-0 py-2 px-4 rounded-xl sm:rounded-full mr-auto sm:mr-0 font-medium transition-all hover:scale-105 text-black active:scale-95 bg-white shadow dark:text-white dark:bg-white/10 animation-background-title-scroll ${classList}`
  onclick="window.scrollTo({ top: 0, behavior: 'smooth' })"
>
  {title}
</button>

<style></style>
